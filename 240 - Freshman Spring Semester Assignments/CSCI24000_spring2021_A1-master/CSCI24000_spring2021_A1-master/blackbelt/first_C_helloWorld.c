#include <stdio.h>

/* It took me 'two days' to write a C program through bash.
 * What I wasn't doing right, was that I didn't know I had to run a.out
 * I knew I had to compile gcc, but I couldn't run the file and got weird syntax errors
 *
 * Know I know, and I can finally promise that this works, if you run a.out.
 *
 * First program in C!!!!!!
 */

int main(){
	printf("Hello World! from me! \n");
	return(0);
} // end main
